export interface ProfileData {
  balance: number,
  birthDate: string | null,
  firstName: string | null,
  lastName: string | null,
  patronymic: string | null,
  passportPhotosUploaded: false,
  referralCode: string,
}
